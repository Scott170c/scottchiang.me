.next
node_modules
out
dist
.vercel
*.log
.env*
!.env.example
tsconfig.tsbuildinfo

# Claude Code session artifacts -- keep launch.json so future sessions can reuse
# the dev-server config, ignore everything else in .claude/.
.claude/*
!.claude/launch.json
